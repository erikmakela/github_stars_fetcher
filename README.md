# github_stars_fetcher
Obtain your github stars in 1 click - a simple CLI and GUI to get your github stars in a list.


CLI: python fetch_stars_cli.py --username githubuser --token ghp_xxxxxxxxxxxxx --output filename --format txt

--username = USERNAME
--token = ghp_xxxxxxxxxxxxx
-output = FILENAME
--format = csv/txt (default markdown)

GUI: